#pragma once

#include <cstdio>
#include <iostream>
#include <stdint>
#include <cstdlib>

using namespace std::string;

#define MIN(a, b)   ((a > b) ? (b) : (a))
#define MAX(a, b)   ((a < b) ? (b) : (a))
